# Independent Life

Independent Life defines what the agent does when the user is not actively talking to it.

Core loops:

```text
wake -> observe context -> explore -> reflect -> update state -> decide whether to share
```

Possible activities:

- read or browse allowed sources
- maintain private notes
- develop interests
- run small experiments
- write an internal journal
- stay silent

The goal is depth, not constant output.

